# Out-of-source CMake builds
/build/
/cmake-build-*/
/out/

# IDE / editor
.idea/
.vscode/
# Obsidian per-vault UI state (workspace layout, plugin cache) when a folder
# in this repo is opened as a vault — local to the viewer, not project content.
.obsidian/
*.swp
*.swo
*~
.cache/
compile_commands.json

# Compiled artifacts
*.o
*.obj
*.a
*.lib
*.so
*.dll
*.exe
*.pdb

# OS
.DS_Store
Thumbs.db
imgui.ini

# Local save slots (P1F)
/saves/*.sav
